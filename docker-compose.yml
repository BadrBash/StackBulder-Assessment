version: '3.8'
services:
  api:
    build:
      context: .
      dockerfile: src/API/Dockerfile
    environment:
      - ConnectionStrings__Default=Data Source=orders.db
      - Serilog__ElasticsearchUri=http://elasticsearch:9200
    ports:
      - "5000:80"

volumes: {}
